import React from 'react';
import {Container} from "react-bootstrap";

const Product = () => {
    return (
        <Container>
            Product
        </Container>
    );
};

export default Product;